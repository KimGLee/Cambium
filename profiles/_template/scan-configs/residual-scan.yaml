# Machine parameters for the residual-content verifier selected by this
# Profile. Tool-owned code defines matching and receipt behavior.

residual_scan_config_version: 1

allowed_roots:
  - TODO(profile)

excluded_roots: []

frontmatter_match:
  field: type
  values:
    - TODO(profile)

heading_match:
  any:
    - TODO(profile)
  combination:
    - TODO(profile)
    - TODO(profile)
  minimum_distinct: 2

mandated_headings:
  - TODO(profile)
