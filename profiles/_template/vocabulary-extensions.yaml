# Interface: profiles/README.md#Vocabulary-Extensions-Slot
# Compose after filling: python3 Tools/compose_vocab.py --extensions profiles/<profile-id>/vocabulary-extensions.yaml

schema_version: 1

# Freeform profile fields without controlled values:
frontmatter_extensions:
  fields: []

fields:
  # Optional kernel-field extension shape:
  # type:
  #   values: []
  # scope:
  #   values: []

  # Conditional expression-readiness field. Replace the mapping key with the
  # field name and point `owner` to its promotion rules. Delete this mapping
  # when no registered artifact uses a readiness axis.
  TODO(profile):
    owner: "TODO(profile)"
    role: Expression Status Axis
    values:
      - TODO(profile)

# domain: fast | slow | stable
volatility_defaults:
  TODO(profile): TODO(profile)
