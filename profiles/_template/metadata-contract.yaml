# Candidate instance values for the Metadata Contract slot. The Kernel-owned
# Profile interface and K08 define the common shape and extension boundary.

schema_version: 1

applicability:
  state: kernel-defaults

applicability_differences: []

extension_fields: []

relationship_extensions: []

section_roles: []
