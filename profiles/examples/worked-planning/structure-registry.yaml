# Worked Planning structure-registry slot: the inactive branch, chosen by the
# K01/05 admission test rather than by corpus size.
# Interface: kernel/K00 Standards Control/profile-interface.yaml
# The four corpus directories are Profile Scope layers with Global Map
# entries; none carries a canonical entry page or is maintained or audited
# independently of the whole, so registering them as units would create
# hollow structure.

schema_version: 2

applicability:
  state: not-applicable
  reason: "A bounded single-service corpus of six pages in four Profile Scope layers; no directory passes the K01/05 module admission test (no canonical entry pages, no independently maintained or audited sub-responsibility), and the support directories are already covered by layer registrations and the Global Map."

units: []

support_layers: []
