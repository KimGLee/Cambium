# Minimal Notes structure-registry slot: the inactive branch.
# Interface: profiles/README.md#Structure-Registry-Slot
# `not-applicable` requires a nonempty reason and empty units and
# support_layers. A flat personal corpus registers no hollow units merely to
# fill the table; see profiles/examples/agent-atlas/ for the configured branch.

schema_version: 1

applicability:
  state: not-applicable
  reason: "One flat folder of personal notes maintained one page at a time; no directory carries a unique responsibility that is built, maintained, or audited independently, so the K01/05 module admission test admits nothing."

units: []

support_layers: []
