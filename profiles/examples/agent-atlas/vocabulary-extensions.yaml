# Atlas values for the Kernel-owned Vocabulary Extensions slot.

schema_version: 1

frontmatter_extensions:
  fields:
    - interview_guide

fields:
  type:
    values:
      - interview-topic-guide
      - roadmap
  scope:
    values:
      - interview-only
  case_class:
    owner: "profiles/examples/agent-atlas/scope-and-architecture.md"
    values:
      - reported-system
      - controlled-study
      - composite-reconstruction
  source_class:
    owner: "profiles/examples/agent-atlas/source-policy.md"
    values:
      - official-engineering
      - paper
      - standard
      - book
      - ecosystem-implementation
      - community-engineering

volatility_defaults:
  modeling-fundamentals: stable
  machine-learning: slow
  deep-learning: slow
  llm: fast
  retrieval-rag: fast
  agent: fast
  ai-systems: slow
  evaluation: fast
  safety-governance: fast
  interview: slow
  knowledge-management: slow
