## Navigation

- Profile: [[profiles/examples/eng-handbook/profile|Eng Handbook Profile]].
- Kernel base dimensions: [[kernel/12 Quality Assurance/01 Quality Dimensions and Single Note Review|Quality Dimensions And Single Note Review]].

## Extension Dimensions

None. This profile registers no extension audit dimensions; the kernel's seven base dimensions apply unchanged to every page.

This is the minimal legal implementation of the slot: the registry file exists so the manifest binding resolves, and it explicitly declares emptiness rather than leaving the slot unbound.

## Interpretation Notes For This Domain

The base dimensions are read against this profile's owners — no new dimensions are created by these notes:

- Depth for Foundations pages is judged by [[profiles/examples/eng-handbook/scope-and-architecture#Foundation Depth Requirements|Foundation Depth Requirements]].
- Depth for Services pages is judged by [[profiles/examples/eng-handbook/scope-and-architecture#Production System Reasoning Applicability|Production System Reasoning Applicability]].
- Provenance for quantitative claims follows [[profiles/examples/eng-handbook/source-policy|Source Policy]] (dashboard plus time window).

## Extension Path

A future extension dimension (for example `runbook-executability`, verified by a periodic game-day exercise) must be registered here with its applicable objects and a single acceptance predicate owner before any gate may consume it.
