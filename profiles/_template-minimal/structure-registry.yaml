# Minimal-depth structure-registry slot: the inactive branch.
# Interface: profiles/README.md#Structure-Registry-Slot
# `not-applicable` requires a nonempty reason and empty units and
# support_layers. A flat corpus registers no hollow units merely to fill the
# table; configure this slot later, through ordinary Standards adoption, when
# a directory first carries a responsibility that is built, maintained, or
# audited independently. See profiles/examples/agent-atlas/ for the
# configured branch.

schema_version: 1

applicability:
  state: not-applicable
  reason: "A flat corpus maintained one page at a time; no directory carries a unique responsibility that is built, maintained, or audited independently, so the K01/05 module admission test admits nothing."

units: []

support_layers: []
