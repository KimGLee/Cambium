# Generated artifact -- do not edit. Every value here is rendered by Tools/render_host_configs.py from one server definition; a hand edit is reported by --check as a HOLD.
#
# destination: <corpus>/.env
# carries: binding
# server name: cambium
# source: Tools/compiled/mcp-tools.json
# source_hash: sha256:65ce7f831b0a67b3a98cbc036b2081defc6d1f805fe615fbb538a608d0ae7221
# regenerate: python3 Tools/render_host_configs.py .
# verify: python3 Tools/render_host_configs.py . --check
#
# This file is the binding half only: which corpus this session governs.
# The registration half is installed once per machine under
# $DSH_HOME/profiles/<name>/.
#
# CAMBIUM_INTERFACE_SOURCE_HASH is the sha256 of the compiled tool
# projection this file was rendered against. It is carried so a server can
# refuse a tool list it was not registered against, and so one upstream
# change makes every host product stale at once.
#
# <CAMBIUM_WORKSPACE_ROOT> still to be substituted. A placeholder is not a
# valid absolute path on any of these hosts, so an un-substituted copy
# fails at launch instead of resolving to something.

CAMBIUM_INTERFACE_SOURCE_HASH="sha256:65ce7f831b0a67b3a98cbc036b2081defc6d1f805fe615fbb538a608d0ae7221"
CAMBIUM_WORKSPACE_ROOT="<CAMBIUM_WORKSPACE_ROOT>"
