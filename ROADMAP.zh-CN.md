# Cambium 路线图

[English](ROADMAP.md) | 简体中文

本路线图描述产品方向。它不是 Kernel 规则、Profile 要求或发布承诺。

只有当某项能力的实现、文档和测试均已存在于仓库中时，该能力才可用。仅凭路线图中的一段描述，绝不代表某项功能已经可用。

## 如何阅读本路线图

每个项目都处于以下四种状态之一：

| 状态 | 含义 |
|---|---|
| **已完成** | 已在仓库中交付，并提供了使用文档 |
| **进行中** | 正在实施；完整变更落地前不可用 |
| **下一步** | 边界和完成测试已经确定的下一项预定能力 |
| **条件性** | 仅在触发条件出现或另行作出产品决策时构建 |

当前面向用户的能力摘要见 [README.zh-CN.md](README.zh-CN.md)。本文件记录接下来要改变什么，以及为什么改变。

## 状态概览

| 能力 | 状态 | 简述 |
|---|---|---|
| Profile 引导流程改革 | 已完成 | 已交付一套预先关闭可选项的模板、脚手架工具、访谈合同、状态视图、检查和端到端测试 |
| 组件职责归位 | 已完成 | Kernel、Card、Read Set、Profile、Tool 和采用方运行时状态各有明确归属；旧的嵌套 Card/Read Set 路径与重复权威来源已被移除 |
| 持久化任务与 Queue 运行时 | 已完成 | 已交付 Coverage、Required Queue、Progress、受控写入器、Receipt、恢复和完成闭环路径 |
| 工作流推进 MVP | 已完成 | 已交付对完全一致候选项的精确沿用、有边界的委托 Amendment，以及已路由缺口的结算机制 |
| 宿主无关的 Agent 接口 | 已完成 | 已交付 CLI 合同、MCP 投影、stdio 服务器和四种宿主配置生成器 |
| 激活传输与 Assignment 交付 | 进行中 | 用有预算约束的交付、宿主符合性证明、确认回执和交付 Gate，替代无法证明的“服务器已经发送”声明 |
| 参考执行运行时 | 下一步 | 将经过交付 Gate 的 Assignment 扩展到执行与检查点生命周期，随后增加单写入者集成器，再增加相互隔离的执行者和审查者 |
| Git 支持的工作区与差异适配器 | 下一步 | 将一个 Assignment 绑定到批次私有的 Git 工作区、可审查差异、具名来源、精确 Git 快照和串行合并后回读，同时不让 Git 成为第二个 Queue |
| 受治理检索适配器合同 | 下一步 | 导出由权威归属推导的可消费清单、限定到当前主体的失效更新流和带有当前性证明的引用封装，同时不让 Cambium 成为 RAG 引擎，也不让索引获得权威地位 |
| 状态感知的操作发现 | 下一步 | 范围已经改变：已交付的 MCP 表面来自工具 CLI，未来任何发现视图都不得成为第二个策略引擎 |
| 类型化依赖运行时 | 下一步 | 编译显式语料库关系并生成有边界的变更影响计划 |
| 独立的完整性与一致性评估 | 下一步 | 不信任执行器自己的 Queue 或 Delta，重新推导预期范围 |
| 机器可读的审查裁决 | 下一步 | 让发现、确认、条件修复和关闭 Gate 证据形成一条可作为治理依据的完整证据链 |
| Receipt 账本完整性链 | 下一步 | 增加带外部尾部锚点且支持按生产者时代重放的前后相连 Receipt 历史 |
| 可观测性与更广泛的 Contract Amendment | 下一步 | 运行时状态和两个 Amendment 字段已经交付；编排视图和其他合同字段仍未完成 |
| OpenAI Plugin 打包 | 条件性 | 仅在每语料库绑定和包生命周期要求得到解决后考虑 |
| 分离式状态事务 | 条件性 | 仅当权威写入器无法在其正常执行通道上完成时需要 |
| 并发 Receipt 封存 | 条件性 | 仅当 Cambium 超出当前单写入者维护窗口时需要 |
| 已封存证据后续工作 | 下一步 | 已修复报告的可达性缺陷；受保护集合推导、再水化和声明式投影仍未完成 |

## 不会改变的权威边界

每个路线图项目都必须保留同一个控制平面：

```text
权威
  = Cambium Kernel
  + 唯一一个已选定的 Profile
  + 采用方拥有的 .cambium 状态
```

Card、Read Set 和 Tool 被有意排除在这一权威等式之外：Card 投影行动指引，Read Set 声明加载边界，Tool 执行或验证既有规则。任何一方都不会因为承担相应职责而成为额外的策略或状态权威。

便利层可以收集决定、渲染视图或调用受控操作。它们不得：

- 发明领域策略或批准 Profile；
- 削弱 Kernel Gate 或绕过 R09 Standards 采用；
- 创建第二个 Queue、Progress 账本或 Receipt 权威来源；
- 暴露任意 shell 执行器或不受限制的仓库路径；
- 未经明确授权运行采用方提供的验证器代码；
- 把检索分数、查询结果、索引或缓存变成策略、依赖、晋升、状态转换或完成权威；
- 在没有提供相应能力的宿主证据时，声称具备已认证身份、隔离、独立审查或交付。

## 已完成的基础能力

这些项目过去属于未来路线图工作。它们现在已经成为当前基线的一部分，保留在这里仅用于清楚展示这一变化。

### 组件职责归位

Cambium 的现有能力现在遵循一套明确的职责划分：

- `kernel/` 拥有通用治理语义、不变量、状态含义、扩展点，以及最小的实现无关合同；
- `Card/` 拥有面向在其他位置选定的路线或阶段的简短、经过人工策划的行动检查单；Card 是非权威投影，在其语义压缩由人工策划时，不会被描述成确定性编译产物；
- `Read Set/` 拥有机器可解析的直接和条件性加载边；其索引是生成式导航，而不是路线或加载权威；
- 一个已选定的 Profile 只拥有已经确认的仓库特定值，而通用 Profile 接口则是一份由 Kernel 拥有的机器注册表；
- `Tools/` 拥有确定性算法、能力实现、工程合同、生成器和受控写入器；
- 采用方拥有的 `.cambium/` 保存当前值、已绑定的运行输入、证据、恢复状态、临时工作和可重建投影。

因此，Card 和 Read Set 与 Kernel 并列放置，而不是位于 Kernel 内部。消费者从相应权威归属方的机器合同中解析其声明，而不是从 README 自然语言、Markdown 标题或重复常量中重建这些声明。仓库文档与测试描述并验证这套已经交付的结构。

这项完成内容是职责纠正，而不是后续的架构重新设计：它不抽离任务类型/路线选择器，不替换所有文件格式，也不决定分发与安装打包形式。

### Profile 引导流程改革

Cambium 现在只交付一套模板，不再区分“minimal”和“full”模板系列。模板预先关闭了具有安全合法退出状态的开关，并将仓库特定决定保持为开放状态。

已经交付的流程包括：

- `profiles/template-files.yaml`，作为精确复制白名单；
- `Tools/scaffold_profile.py`，用于安全创建候选 Profile；
- `profiles/interview.yaml`，作为机器可读的问题合同；
- `Tools/profile_onboarding_status.py`，作为只读状态与 `next_action` 视图；
- `Tools/check_profile.py`，用于结构与依赖验证；
- 面向现有语料库和空语料库的端到端测试夹具。

辅助 Agent 可以准备候选 Profile，但不能批准或选定 Profile，也不能推断未经确认的领域策略。

过去设定的“在 30 分钟内完成至多 15 项操作者决定”目标现在属于产品体验测量，而不是实现声明。在真实采用方环境中完成测量之前，它仍未得到证明。自动化访谈执行器可以作为一种便利层，但它不是缺失的治理机制。

### 持久化运行时与工作流推进

采用方拥有的运行时现在区分：

- Coverage 中的对象状态；
- Required Queue 中的批次状态；
- Progress 中整个任务的状态；
- 不可变 Work Spec 中的复杂批次指令；
- Delta 与受控计划中的拟议变更；
- Receipt 中的只追加证据。

此外，三项工作流债务已经关闭：

1. `exact-carry-v1` 只复用紧邻的上一次成功关闭中，对完全相同且未变化观察项作出的审查处置。
2. `amendment_authority` 只能委托已登记且有边界的运行变更类别；委托不能自行扩张。
3. 路由到某批次的每个缺口都必须在该批次达到 `merge-ready` 前得到结算或重新路由。

这些机制复用现有账本和写入器，不会创建并行控制平面。

### 宿主无关的 Agent 接口

宿主无关接口已经完成：

```text
每个工具的 argparse 声明 + 封闭的 Agent 接口策略
  -> Tools/compiled/cli-contract.yaml
  -> Tools/compiled/mcp-tools.json
  -> Tools/mcp_server.py
  -> 生成的宿主配置
```

Claude Code、Codex、Kimi Code 和 dsh 使用生成的注册与工作区绑定。MCP 服务器以子进程方式运行 Tool 并原样传递结论。它让一个已经打开的权威工作区目录对象跨越子进程边界保持固定，并要求每项投影操作和每条实际生效的文件系统路径——包括被省略的 CLI 默认值——都位于声明的能力边界内。封闭策略还为每条类型化路径指定一种消费模式：`snapshot`、`append`、`replace` 或 `transaction`。

对于一次 MCP 调用，传输层保留获准的目标和/或稳定父目录描述符，将一份与内容绑定的能力清单和这些描述符一并传给子进程，并要求每条活动类型化路径确认自己确实通过描述符完成了消费，之后才可返回通过或暂缓结果。共享 I/O 基础操作在读取、可能创建文件的发布、追加、原子替换、事务自回读和嵌套 Cambium 子进程中持续保留同一个对象。两个活动参数不能在相同消费模式下指向同一路径，因为一次使用无法证明究竟消费了哪个语义输入。声明的操作模式可以启用或停用原本有效的输出默认值；该条件谓词属于编译后接口的一部分，而不是实现层的猜测。若宿主平台不具备禁止跟随符号链接的目录描述符和基于描述符的相对子进程执行能力，初始化将失败。

这是对公开调用面的能力约束，不负责判断某项操作是否获准，也不判断证据是否充分。仅供 CLI 使用的构建和安装操作永远不会进入 MCP 表，其已注册的外部影响仍保持显式。受保护的完整工作区执行仍属于独立的适配器事项：固定或派生的内部路径、仓库代码与证据的一致重写，以及文件系统能力边界之外的影响，都不会被提升为该传输机制的保证。

这次交付改变了 Plugin 计划：Cambium 不再需要 OpenAI Plugin 来获得可调用的 Agent 接口。Plugin 只会增加打包与分发能力。

### 封存证据可达性修复

Receipt 封存现在会保留已记录 Queue 转换所消费的证据，从已验证的冷存储中解析所需正文，并在热存储与冷存储都不存在证据时失败关闭。

这修复了 Receipt 封存可能在无提示的情况下，让一项已经履行完毕的义务重新变为未完成的问题。更广泛的后续工作列在 [已封存证据加固](#sealed-evidence-加固)中。

## 进行中

### 激活传输与 Assignment 交付

**状态：进行中；在完整的标准、工具、生成产物和测试一并落地前尚未交付。**

最初的激活协议将 MCP 会话收到的服务器结果，视为模型上下文已经收到完整 Card 集合的证明。一次真实宿主测量表明，过大的结果可能被外置，而 Receipt 仍然声称已完成机器交付。服务器能够证明自己发送了什么，却不能证明宿主把什么内容放进了上下文。

替代设计将四个事实拆开：

1. Queue 准入冻结精确的 Card 与启动 Read Set 清单。
2. 内容在经过测量的结果大小预算下，一次传输一个完整文件。
3. 接收方执行上下文为每个已交付片段返回一个 nonce。
4. 一条当前有效的 Host Adapter 符合性记录证明预算内的结果以内联方式交付，而不是被截断或外置。

随后，Assignment 按以下路径推进：

```text
pending -> delivering -> delivered -> running
```

只有 Assignment 交付 Gate 才能授权进入 `running`。Queue `open` 仍然表示批次已经准入；它不表示执行者已经收到其 Card。

交付证据绑定到一个 Assignment、一个执行上下文、一个 Card 集合和一次交付尝试。重新指派或 Profile/Card 集合变化都要求重新交付。即使 `delivered` 状态有效，它也只证明完成了交付，并不证明 Agent 已经阅读、理解或遵守这些材料。

本项目在满足以下条件时完成：

- 片段预算来自可复现的正向与负向宿主测量；
- 过大的叶级文件在准入期间失败，而不是在交付过程中失败；
- 每个片段都完成哈希绑定，并在同一上下文中交付和确认；
- 当前有效的、版本化 Host Adapter 符合性记录存在；
- 持久化 Assignment 写入器和 Gate 消费完整的确认集合；
- 恢复和重新指派会使旧交付证据失效；
- 不受支持的宿主回退到显式降级状态；
- 生成的 CLI/MCP 产物和负向测试夹具与新协议一致。

## 下一步能力

### 参考执行运行时

Cambium 定义了批次生命周期和串行集成，但目前还不会运行 Agent。参考运行时将消费现有 Required Queue，而不是发明一套由调度器拥有的账本。

激活项目拥有持久化交付记录和交付 Gate。参考运行时将同一个 Assignment 扩展到执行与检查点生命周期；它不会创建另一种 Assignment 类型，也不会把交付重新表述为一项“下一步”能力。

交付顺序如下（相邻阶段可以重叠；只有下游完成条件也一并落地，一项能力才算完成）：

1. **执行生命周期 Assignment 扩展**——为一个经过交付 Gate 的 Assignment 增加临时执行上下文、角色、写入范围和检查点。
2. **Git 支持的工作区与差异基础操作**——将该 Assignment 绑定到精确的基础提交与目录树、批次私有 Git 工作区、获准的写入表面、可审查差异，以及可恢复的前后身份。
3. **单写入者集成器与适配器完成**——准入已经就绪且互不重叠的批次，收集 Delta 与 Receipt，通过适配器一次合并一个批次，回读合并后的目录树，并在每次合并后运行全局检查。
4. **隔离的执行者**——每个活动批次只有一个写入归属方，并使用批次私有输出。
5. **全新上下文审查者**——只接收适用审查合同所要求的审查输入。
6. **恢复与可观测性**——提供取消、中断、重新指派、冲突、超时和交接诊断。

步骤 2 使拟议的仓库影响可供审查。Git 适配器在步骤 3 将串行应用与合并后回读绑定起来之前，仍处于“下一步”状态；仅凭列表位置不构成完成声明。

活动批次数量上限仍与 Agent 上下文数量相互独立。Host Adapter 必须声明不受支持的隔离、取消、身份或文件系统能力，并安全回退。

当并行且互不重叠的工作可重放、共享集成保持串行、中断工作能够从持久状态恢复，并且任何参与者、审查者、交付或隔离声明都不超出宿主证据时，此能力即告完成。

### Git 支持的工作区与差异适配器

**状态：下一步；它是参考执行运行时下的一种参考适配器，而不是新的控制平面。**

Git 已经拥有版本历史、文本差异、提交、目录树和回滚。Cambium 不得重新创建这些机制，也不得把分支当作第二个任务账本。Required Queue 仍是唯一的权威批次生命周期，Assignment 仍是执行上下文记录，Receipt 仍是证据历史。适配器的工作，是将这些现有权威绑定到可审查的仓库影响。

参考流程如下：

```text
Required 批次 + 持久化 Assignment
  -> 声明的基础提交和目录树
  -> 批次私有分支与工作树，或专用克隆
  -> 执行者在获准清单内进行变更
  -> 与具名来源绑定的可审查差异
  -> 批次本地检查与 Delta/Receipt 发布
  -> 单写入者串行集成
  -> 精确回读合并后目录树并运行全局检查
  -> 如获授权，仅由集成器远程发布并回读远程 ref
```

对于每次尝试，适配器至少会记录或绑定：

- 仓库身份、声明的基础提交和基础目录树；
- Assignment、任务、批次、Work Spec、获准的清单和允许路径；
- 变更声称使用的具名来源或来源 Receipt ID；
- 分支与工作树身份，但不把其名称当作经过认证的参与者身份；
- 工作区能力模式和共同 Git 管理边界，包括共同目录身份、受保护及适配器拥有的 ref、配置、钩子、备用对象库、替换 ref、工作树清单和对象暂存模式；
- 当远程操作在范围内时的权威远程仓库身份、受保护的远程 ref、远程 URL 与辅助程序配置，以及当前凭据与网络能力模式；
- 头部提交、结果目录树、权威差异字节或补丁 ID，及其哈希；
- 与获准写入表面有关的已修改、未跟踪、已忽略、子模块、符号链接、硬链接和不安全文件观察；
- 精确的合并或应用结果、权威的合并后提交与目录树回读，以及针对所得仓库状态运行的全局检查 Receipt。

执行者可以提出提交或差异，但不能推进共享 Queue、Progress、Standards、Profile、集成状态、权威 ref 或共享 Git 管理状态。其 Git 写入能力仅限获准的工作树表面，加上适配器拥有的批次 ref 和私有或隔离的对象暂存区域。它不得修改受保护 ref、共享配置或钩子、备用对象库、替换 ref、另一个工作树的元数据或对象库维护状态。它也不得保留能够更新受保护远程 ref 的可写权威远程仓库、凭据、远程辅助程序路径或网络路由。这种缺失必须是宿主提供证据的能力边界，而不能只是编码在本地 Git 配置中的约定。

仅当当前宿主证据能够证明这些共享本地和远程修改对执行者不可用时，才支持链接工作树；否则适配器必须使用具有等价绑定的专用克隆，或返回 `unsupported`。

只有逻辑集成器可以接受一次当前尝试，将其应用到当前权威目录树，回读所得目录树和受保护的 Git 管理状态，运行所需全局检查，并推进现有生命周期。明确授权远程发布时，也只有集成器可以执行；适配器必须绑定精确的远程 ref 前映像、更新结果和权威的推送后远程 ref 回读。某个 Tool 或 Agent 操作记录——或者成功的本地 `git push` 退出状态——声称写入或合并成功，并不能证明最终的本地或远程仓库状态。

适配器必须针对陈旧基础、超出清单的路径、已修改或未绑定的影响、部分提交、缺失具名来源绑定、差异变化、意外目录树、未解决文本冲突、中断合并或合并后回读不匹配而失败关闭。对于未绑定的受保护 ref、配置、钩子、备用对象库、替换 ref、工作树清单或对象暂存变化，以及让受保护远程写入成为可能或产生歧义的远程仓库、配置、凭据或网络漂移，它也必须失败。恢复过程必须保留分支、工作树或专用克隆、锁、差异、隔离对象和前后身份，直到集成器能够核对该尝试；有歧义的远程更新需要权威远程 ref 回读。仅仅因为 Assignment 被取消，清理过程绝不能丢弃尚未集成的用户或 Agent 字节。

Git 提供可见性和回滚能力，而不是语义治理。无冲突合并不能证明正确性、完整性、审查者独立性或参与者身份。尤其是当两个 Agent 在不同文件中编辑同一个概念时，Git 可能在没有文本冲突的情况下完成合并。权威归属、类型化依赖、来源审查、跨文件一致性检查和完成 Gate 仍属于 Cambium 的职责。外部数据库或 API 副作用不在此适配器范围内。只有在显式启用远程模式和仅由集成器回读的合同时，Git 远程 ref 更新才受其覆盖；任何其他外部系统适配器都必须执行权威的操作后回读，并在声称成功前绑定该观察。

本能力在满足以下条件时完成：

- 一个已准入的 Assignment 能够从精确声明的基础创建或恢复一个批次私有 Git 工作区，而不修改操作者的工作树，也不修改任何受保护或不归适配器所有的共同 Git 管理状态；
- 适配器拥有的批次 ref、工作树清单条目和对象暂存区域具有已绑定的创建、使用、恢复和清理生命周期；
- 执行者尝试不存在指向权威远程仓库的可写路径；与此同时，经授权的集成器发布会绑定精确的远程 ref 前后身份和权威的推送后回读；
- 允许路径和具名来源检查将确定性、可审查的差异绑定到其 Assignment、批次、Work Spec、基础提交与目录树，以及头部提交与目录树；
- 集成器能够把一个已接受尝试串行应用到当前权威目录树，回读精确结果并将全局检查绑定到该结果；
- 陈旧基础、路径逃逸、已修改状态、未跟踪或已忽略文件、中断提交或合并、文本冲突和合并后漂移测试夹具，在不丢失可恢复字节的情况下失败；
- 尝试执行受保护 `update-ref`、共享配置或钩子注入、备用对象库或替换 ref 变更、外部工作树元数据变更和破坏性对象库维护时，必须失败且不影响权威仓库或其他 Assignment；
- 执行者上下文中的普通推送、强制推送、受保护远程 ref 删除、远程 URL 重写，以及绕过远程辅助程序或凭据的测试夹具均失败；
- 文档明确说明 Git 检测的是仓库文本变更，而不是跨文件语义冲突或完整任务完成；
- 任何 Git 分支、提交消息、作者标签、生成图、差异视图或适配器记录，都不会成为第二个 Queue、Progress 账本、Receipt 权威、Profile 权威或身份证明。

### 受治理检索适配器合同

**状态：下一步；它是一种下游消费适配器，而不是 RAG 引擎、知识库或第二个知识状态权威。**

Cambium 治理仓库工作、证据、恢复和关闭，但目前尚未定义外部搜索、RAG、Wiki 或 Agent 运行时如何消费所得受治理语料库。否则，每个采用方都必须自行确定哪些对象可以纳入、如何排除陈旧或失效证据、索引如何映射回精确的权威字节，以及变更后必须重建什么。这种重复会造成权威不一致，也让治理价值难以在下游使用中观察。

该合同会基于现有权威模型暴露一个确定性的受治理检索视图：

```text
Kernel + 已选定 Profile + 采用方状态 + 精确语料库快照
  -> 类型化、与权威归属绑定的准入投影
  -> 可消费清单 + 单独获授权的排除诊断
  -> 限定到主体的变更与失效更新流
  -> 可丢弃的外部词法、向量、图或混合索引
  -> 针对清单、索引与权限水位的已证明当前性 Gate
  -> 带精确权威引用封装的检索结果
  -> 用户或 Agent 消费方
```

准入投影派生自现有权威归属方。它不会增加通用 `accepted` 字段、由检索拥有的状态账本或隐式晋级路径。其策略所有权是明确的：

- Kernel 只拥有协议底线：精确快照绑定、对不受支持状态失败关闭，以及检索产物不具备治理权威这一规则；
- 已选定的 Profile 通过一种类型化、已注册的策略，拥有语料库特定的检索准入规则；该策略把现有权威事实映射为可纳入、不可纳入或仅供诊断的结果；
- 已选定的 Profile 还在宿主权限底线内拥有类型化、已注册的封装字段删减和聚合诊断披露策略；该策略可以收窄披露范围，但绝不能扩大宿主访问权限；
- Coverage、属性、证据、来源、主张和失效权威归属方继续拥有自己的事实；检索策略可以读取，但不能重新解释或重写这些事实；
- 宿主拥有身份与资源或字段权限证据；适配器可以投影该证据，但不能发明主体、字段授权或访问决策。

如果已选定的 Profile 对某个对象类别不存在合法检索策略，或者所需权威事实缺失或有歧义，准入结果为 `unsupported` 并失败关闭。适配器不得根据 `authoring_status`、证据成熟度、时效性、流行度或检索分数推断 `accepted`。

该合同至少必须绑定：

- 语料库与工作区身份，以及精确的文件系统或仓库快照；
- 当前 Standards 和已选定 Profile 的身份；
- 权威对象身份、路径、精确范围和内容哈希；
- 决定纳入或排除的权威 Coverage、属性、当前性和失效事实；
- 在采用方 Profile 提供相应内容时的已注册来源与主张引用，以及由其他权威归属方独立拥有的披露与权限事实；
- 声称具备权限感知检索时的 Host Adapter 符合性身份、租户与工作区边界、调用方或主体身份、资源权限快照或时代、观察时间、有效期与过期时间，以及执行模式；
- 无法证明当前身份或权限证据时的显式不受支持结果；只有在更窄边界及其授权证据均已命名时，才可以使用预先限定的可信语料库，并且绝不能借此声称实施了用户特定的权限执行；
- 已注册封装字段删减和聚合披露策略的身份，包括任何获准分组和最小分组规则；
- 每个实际使用层的适配器、清单模式、分块器、嵌入模型、图和重排器身份。

适配器将定义四种可互操作输出：

1. **可消费清单**——只包含类型化策略和当前权限证据准入的对象，并带有精确的字节与范围身份、适用的纳入理由、快照身份、`manifest_id` 和权限时代。
2. **限定到主体的变更与失效更新流**——两个清单之间的确定性新增、替换、移除、权限撤销、证据失效和重建索引范围，并带有 `from_manifest_id`、`to_manifest_id` 和单调递增的更新流水位。撤销记录可以标识一个过去可见的对象，以便消费方将其删除；一个从未可见的未授权对象不得出现。
3. **引用与结果封装**——返回的段落，加上权威对象、范围与哈希，当前快照和 Standards/Profile 身份，可用的来源与主张引用、准入事实、检索轨迹、已应用的 `manifest_id`、索引水位、权限时代，以及两个类型化状态：`citation_status` 表示对所引对象的权威字节与支配事实进行验证的结果，`retrieval_snapshot_status` 则表示检索器所应用受治理视图的状态，例如 `current`、`stale`、`unverified` 或 `unsupported`。每条来源与主张引用、准入字段和候选级轨迹条目，都必须独立获得对当前主体的授权；否则，由已注册策略将其省略或删减，而缺失策略就表示不得披露辅助字段。
4. **已授权的排除诊断**——面向审查者与操作者的独立输出，绝不属于普通可消费清单。只有当前宿主证据和已注册 Profile 披露策略授权时，才显示对象身份、路径、原因和数量。在没有对象级权限时，只有精确分组和最小分组规则得到授权，才会输出聚合信息；缺失策略、缺失证据或零可见主体都会产生零诊断披露。

外部索引给出的一条简单确认只是一项声明。为了授权 `retrieval_snapshot_status=current`，已应用视图证明必须绑定：

- 版本化检索器与 Host Adapter 符合性身份；
- 精确清单、权限时代、更新流水位、执行模式、适配器与分块器身份，以及结果索引身份；
- 确定性的应用后回读或内容清单摘要，用于证明哪些受治理对象存在且为当前状态，以及哪些已撤销对象已被移除；
- 证明方法、观察时间、有效期与过期时间，以及证据引用。

当前性被有意拆开：

- 引用与对象的当前性，可以通过对精确引用字节、支配事实和当前权限进行权威穿透验证来确立；
- 检索快照的当前性需要一份当前有效的已应用视图证明，与当前受治理清单、权限时代、更新流水位、执行模式，以及适配器与索引身份匹配；
- 对一条返回引用进行穿透验证，不能证明新获准对象已被索引，因此它可以产生当前的 `citation_status`，而 `retrieval_snapshot_status` 仍为 `stale` 或 `unverified`；封装绝不能把二者合并成一个不加区分的 `current` 裁决。

该证明是消费证据。它不是权威事实、权限权威、语义完整性证明、召回率证明或排序质量权威。

外部检索器可以使用 BM25、嵌入、图遍历、重排或其组合。这些机制只属于发现与排序输入：

- 检索分数不是证据成熟度、事实置信度或晋级权威；
- 检索遗漏不能证明知识不存在；
- 语义相似性不会创建依赖边或失效规则；
- 索引、缓存、分块存储或生成图都是可丢弃、可重建的产物，绝不是权威知识；
- 查询或回答不得改变 Coverage、Queue、Progress、Profile、Standards、Receipt、页面状态或完成状态；
- 只有当前宿主证据绑定调用方和来源权限时，才可以声称具备权限感知检索；否则适配器将失败关闭，或只暴露明确授权的预先限定语料库，且不声称执行用户特定的权限控制。

Cambium 不会在本能力中构建通用连接器、OCR、文档解析、向量数据库、相关性基础设施、对话界面或模型网关。小型参考适配器和符合性测试夹具可以使用一种词法路径和一种向量路径，但仅用于证明合同。外部系统仍负责摄取机制、索引、检索、排序和答案生成。

本能力可以与参考执行运行时并行推进。当 Git 是工作区时，Git 支持的适配器提供精确仓库目录树，但 Git 并非必需：采用普通文件系统的采用方必须能够绑定等价的精确快照。类型化依赖运行时之后可以提高变更影响精度；v1 可以只消费显式登记的关系和失效信息，且不得从相似性推断缺失的权威。

本能力在满足以下条件时完成：

- 相同语料库、策略、Host Adapter 符合性、主体、权限时代和适配器输入，产生逐字节相同的可消费清单，以及限定到主体的变更与失效更新流；
- 每个返回段落都解析到精确的当前权威对象、范围、内容哈希、语料库快照和支配准入事实；
- 由类型化 Profile 策略判定为不可纳入的测试夹具不出现在可消费清单中；从未可见的未授权对象不会泄露身份、路径、范围、支配原因、来源与主张引用、轨迹身份或聚合信号，而授权诊断和撤销墓碑所披露的内容不超过其已证明范围；
- 内容、状态、证据、Standards、Profile 或权限变化会产生所需移除或重建索引事件；任何清单 ID、更新流水位或权限时代不匹配、已过期权限证据，或者缺失或过期的已应用视图证明，即使引用穿透验证通过，也会阻止 `retrieval_snapshot_status=current`；
- 至少一个可丢弃的参考索引可以从可消费清单删除并重建，而不会丢失权威、来源脉络或排除语义；
- 一套参考外部检索器测试夹具证明清单摄取、增量失效、应用后证明、权限撤销、对陈旧状态与简单确认的拒绝、引用验证与往返校验，以及失败关闭行为；
- 至少包含两个授权范围不同主体的测试夹具，证明页面可见而来源隐藏时的删减、对未授权检索候选的抑制、零披露聚合行为，以及权限撤销后有范围的墓碑和诊断行为；
- 适配器不暴露共享状态写入操作，负向测试夹具证明 Cambium 受控写入器与 Gate 会拒绝把检索分数、相似性、缓存结果、封装和 Agent 回答，当作晋级、依赖、状态转换、Receipt、证据复用或完成权威；
- 文档和生成接口产物描述同一份合同，并显式说明不受支持的身份、访问控制列表、来源和检索能力。

### 状态感知的操作发现

早期路线图提出用一份 Operation Capability Registry 作为 MCP 工具列表的来源。交付过程证明，两个不同问题被混在了一起：

- **此发行包可以调用什么？** 已交付的编译后 CLI 合同拥有这一答案。
- **此状态接下来可以做什么？** Kernel 规则与每个受控 Tool 拥有这一答案；`check_queue --resume-status` 投影 `next_action`。

现有 `Tools/operation-capabilities.yaml` 的职责更窄：它将元数据字段和转换绑定到已安装的写入器、消费方、生产者和 Receipt 模式。K00/12 另行拥有 Gate 能力与重新验证映射。二者都不是通用任务状态权限表。

只有运行时确实需要能力发现视图时，未来才有理由构建它。如果构建，它必须将编译后 CLI 合同与当前状态组合起来，返回一组可解释的允许操作。它不得手工列出 Tool、取代写入器验证，也不得成为第二个转换权威。

本项目不再是 MCP 表面或 Plugin 打包的前置条件。

### 类型化依赖运行时

Cambium 已经能够验证显式 Corpus Planning 输入和 Profile 依赖闭包。缺失的能力是一个宿主无关的语料库关系与变更影响编译器。

可接受输入包括显式 Global Map 依赖、Capability 与 Gap 链接、前置元数据依赖项、权威对象到派生对象的绑定、来源与主张绑定、MOC 成员关系、模式、Profile、Standards，以及已注册的关系扩展。

编译器将生成确定性图。每条边都必须命名：

- 依赖对象与被依赖对象；
- 关系类型与失效策略；
- 授权该边的声明。

变更影响规划器将比较已接受快照与候选快照，解释每个受影响对象由哪条边导致，命名失效的质量维度，并指出所需检查或审查。

原始反向链接和语义相似性是发现输入，而不是依赖权威。运行时不得重写内容、提升状态、执行无边界审查或宣布完成。

当相同输入产生逐字节相同的图、每条已声明边都能够解析或具有已授权处置、直接影响完整且可解释、普通导航链接不触发失效，并且普通文件系统与宿主支持的测试夹具产生等价语义时，此能力即告完成。

### 独立的完整性与一致性评估

当前 Gate 能够证明已声明的 Coverage、Queue、Delta、Receipt 和快照状态相互一致。它们不能独立证明这些声明覆盖了完整的预期语料库。

新的评估器将首先从上游已接受输入推导预期集合，例如 Task Contract、Profile 范围、Corpus Planning 产物、仓库快照、已接受 Amendment 和类型化依赖图。随后，它会将该集合与 Coverage、Queue 清单、Delta、变更文件、失效信息和 Receipt 比较。

它不得把执行器的 Queue、Delta、变更文件清单或完成声明，当作应当存在哪些内容的权威。

同一个只读边界还会执行有边界的跨文档检查，识别相互冲突的定义、默认值、阈值、归属、机制和陈旧下游结论。先运行确定性比较；语义审查仅限显式概念组，并返回精确段落与理由。

当一个内部一致但只包含真实预期集合一半内容的集合无法通过，缺失或有歧义的输入不能通过，空范围需要独立可验证谓词，并且评估器不能修改状态或批准自己的发现时，此能力即告完成。

### 机器可读的审查裁决

Batch Review Requirement 和判断 Receipt 现在提供了一套机器可读的批次级基础。K12/12 的实质性审查发现和确认裁决仍以文字表达。

承重顺序必须是：

1. 一个审查上下文写入稳定发现 ID、等级、目标和已判断字节。
2. 一个不同的确认上下文为每项发现写入一个裁决。
3. 一个条件修复写入器落实该裁决，检查前映像，在锁内应用字面且有边界的补丁，并写入 Receipt。
4. 批次关闭 Gate 拒绝尚未解决或尚未执行的条件性发现。

在第一个生产者存在之前，第二轮 `not-closed` 结果会触发升级。由执行者创建、但既没有独立生产者写入、也没有关闭 Gate 消费的字段，无法解决问题。

### Receipt 账本完整性链

Receipt 是只追加 JSONL，许多授权字段已经与状态、计划或锚点交叉绑定。仍有一类结构只有一个持久载体，因此不能证明自身历史值。

计划中的完整性层会增加：

- 每个链式生产者时代 Receipt 上的 `prev_receipt_sha256`；
- 第一行声明的创世值；
- 写入到追加该 Receipt 的权威状态事务中的外部尾部锚点；
- 生产者时代重放，使链之前的 Receipt 按其原始规则继续有效；
- 对尾部不确定与后缀损坏失败关闭并恢复。

没有外部锚点的链并不足够，因为写入器可以重建整个文件。此功能增加取证完整性；它不会授予新的权威，也无法抵御能够同时重写状态、Tool、计划和证据的对手。

### 可观测性与 Contract Amendment

状态级可观测性已经通过 `check_queue --resume-status` 和派生 Queue 报告交付。参考运行时仍需要面向 Assignment、Agent 上下文、交付尝试、冲突、取消、超时和交接的视图。

受保护的 Contract Amendment 写入器当前支持：

- `policy_exceptions`；
- `amendment_authority`。

目标、排除项、验收条件、时间安排和暂停策略仍需要后续任务。只有具备显式权威规则、完整前后绑定、锁内重新验证、恢复和消费方测试，一个字段才能获得受保护事务。通用的任意 Contract 差异并非目标。

### 采纳恢复加固

Atlas 本次采用的受支持路径是干净、单进程的硬切换事务；旧 runtime 兼容不是其前置条件。后续应由独立 Cambium 任务测试并闭合硬中断恢复，包括：`committing` journal 发布或重建最终 Receipt 之前，必须精确回读 planned-after 状态，并重新执行 selected Profile CAS。并发、冷状态迁移和旧协议修复仍不属于该任务，除非另行明确立项。

### Python 入口执行打包

后续执行打包任务需要把所有被直接调用的 Python Tool 统一路由到一个只依赖 stdlib、隔离缓存的 launcher。本轮有界修复只保护采纳/checker 与 carried MCP 启动路径；它不重定义全部 source-distribution 开发命令，也不提前决定后续安装形式。

### Sealed-evidence 加固

最初的热存储到冷存储可达性缺陷已经修复。仍有三项更广泛的债务：

1. 受保护的热存储引用集合仍由人工枚举，而不是从消费方推导。
2. 尚无受认可的复原路径将所需冷存储行移回热存储状态。
3. 冷存储投影模式仍为固定形式，而不是由消费方声明。

这些是有边界的后续工作。它们必须扩展现有已验证的热存储与冷存储目录，而不是引入第二个 Receipt 存储。

## 条件性扩展

### OpenAI Plugin 打包

Cambium 已经能够通过生成的 MCP 配置运行。因此，OpenAI Plugin 是可选的打包与分发层，不属于 Cambium 核心或参考运行时。

经测量的 Agent Plugins 命令行环境当前无法在握手阶段建立采用方的逐语料库工作区绑定：它不暴露根目录，省略工作目录时会解析到 Plugin 资源根目录，也不能转发环境白名单。因此，Codex 目前使用项目级 `.codex/config.toml`。

只有当该绑定在上游得到解决，或存在另一种显式且经过测试的载体时，Plugin 工作才应恢复。如果恢复，首个软件包应包含一个 `cambium` Plugin，并基于现有宿主无关接口提供小型 `adopt`、`operate` 和 `audit` Skill。

必需边界如下：

- 安装、更新、降级或移除绝不改变采用方拥有的 Standards、Profile、任务状态或 Receipt；
- Plugin 缓存只保存可丢弃的偏好、索引和缓存；
- 不提供任意命令运行器或不受限路径访问；
- 采用过程生成候选和差异，绝不自动批准；
- 仓库提供的验证器需要经过披露和明确授权；
- 软件包内容从权威来源生成，并具备正确的逐路径许可与归属说明。

任何发布之前，都应对照当前 [OpenAI Plugins 文档](https://developers.openai.com/plugins)和 [构建器合同](https://developers.openai.com/plugins/build/plugins)完成验证，然后测试真实安装、新任务加载、升级、回滚和移除。

公开市场分发、远程 MCP、界面和工作区共享仍属于更后续的产品决定。它们不定义宿主无关运行时是否完成。

### 分离式状态事务

仅当权威执行通道无法让状态写入器完成时，才需要此协议。

安全设计如下：

1. `detached prepare` 锁定真实权威命名空间，并记录完整前映像，包括状态文件、Receipt 尾部、待处理 Delta、归档移动和锁。
2. 另一个环境严格依据该前映像计算。
3. `detached commit` 在仍被持有的真实锁内重新检查完整前映像，只追加新的 Receipt 字节，并安装显式后映像。
4. 任何漂移都会中止并进入正常恢复。

把状态复制出去、在别处计算，然后只依靠三个状态文件哈希将其复制回来，并不是可复用协议。它不能保护权威 Receipt 边界或并发命名空间变更。

### 并发 Receipt 封存

当前 Receipt 封存有意要求一个只有一个写入器的维护窗口。追加互斥锁可以捕捉普通的竞争追加者，但并不声称提供跨宿主、对抗性或完全并发的排他保护。

只有当 Cambium 决定扩大这一边界时，才需要真正的并发版本。它需要所有追加者都参与的共享时代或切换协议、显式排他不变量、跨宿主恢复，以及真实竞争写入器测试。

这不是对当前标记的渐进加固。在产品边界发生变化前，单写入器维护窗口仍是受支持的合同。

## 已调整或退役的方向

若无新决策，不应恢复以下较早方向：

- **Plugin 优先交付。** 宿主无关 CLI/MCP 接口已经先行交付；Plugin 工作现在属于可选打包。
- **把一份通用 Operation Capability Registry 作为 MCP 来源。** CLI 合同拥有可调用接口形态；状态权限仍属于 Kernel 规则和受控 Tool。
- **并行的 minimal/full Profile 模板。** 已退役。`profiles/_template/` 是唯一候选模板；用户在访谈中决定回答深度，并回答所有适用的扩展问题包。
- **由调度器拥有的批次账本。** Required Queue 仍是唯一的权威批次生命周期。
- **把 Queue 准入当作执行者交付的证明。** 准入与 Assignment 交付是两个不同事实。
- **自动执行采用方验证器。** 验证器代码保持披露并需要明确授权。
- **把提示行为当作符合性证据。** Skill 和提示引导使用；Gate、写入器、Receipt 和宿主证据承载声明。

## 交付顺序

当前关键路径是：

```text
激活传输保障
  -> 持久化 Assignment 交付状态与 Gate
  -> Assignment 执行生命周期扩展
  -> Git 支持的工作区与可审查差异基础操作
  -> 单写入者集成循环与适配器合并后回读
  -> 隔离的执行者
  -> 全新上下文审查者
  -> 取消、重新指派和编排可观测性
```

三条工作线可以并行推进：

```text
显式规划输入
  -> 类型化依赖图
  -> 变更影响计划
  -> 独立的预期集合与一致性评估

批次级审查证据
  -> 逐项发现的审查裁决
  -> 条件修复写入器
  -> 关闭 Gate 消费

受治理语料库准入投影
  -> 可消费清单与限定到主体的变更及失效更新流
  -> 可丢弃的参考适配器与应用后回读及证明
  -> 查询时的已证明当前性 Gate 与引用及结果封装
  -> 外部检索器符合性
```

受治理检索工作线增加一个下游接口，但不改变当前执行运行时关键路径。将它列为“下一步”不会自动启动实现，也不表示存在额外资源；安排它需要在其他“下一步”能力之间明确决定优先级与资源分配。

Receipt 链完整性、Contract Amendment 扩展和 Sealed-evidence 加固是相互独立的控制平面改进，但每一项都必须保留生产者时代重放和现有权威边界。

Plugin 打包、远程 MCP、界面和生态分发仍属于下游选择。它们都不是完成宿主无关 Cambium 运行时的前置条件。

## 完成定义

任何路线图项目都不会仅因为正常路径可以工作就算完成。完成需要：

- 已跟踪的实现和当前用户文档；
- 确定性或具有显式边界的语义权威；
- 在适用时，覆盖陈旧状态、格式错误输入、缺失证据、路径逃逸、中断和不受支持宿主能力的负向测试夹具；
- 保留证据而不是进行猜测的恢复行为；
- 重新计算并检查生成产物；
- 不得在适配器或视图中隐藏新的策略权威归属方、账本或信任声明。
